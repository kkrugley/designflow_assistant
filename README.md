# Designflow Assistant: Ваш Персональный Ассистент Дизайнера в Telegram

**Designflow Assistant** — это приватный Telegram-бот, созданный как персональный ассистент для промышленных дизайнеров и креативных специалистов. Он помогает систематизировать идеи, управлять проектами от замысла до архивации и автоматизировать создание презентационных материалов, работая исключительно для вас.

---

### 📜 Оглавление

*   [🚀 Функционал и возможности](#-функционал-и-возможности)
*   [🛠️ Технологический стек](#-технологический-стек)
*   [📋 Что необходимо для работы](#-что-необходимо-для-работы)
*   [⚙️ Локальная установка и запуск](#-локальная-установка-и-запуск)
*   [☁️ Развертывание (Деплой) на Heroku](#-развертывание-деплой-на-heroku)
    *   [Шаг 1: Подготовка файлов проекта](#шаг-1-подготовка-файлов-проекта)
    *   [Шаг 2: Настройка Heroku](#шаг-2-настройка-heroku)
    *   [Шаг 3: Запуск и дальнейшая работа](#шаг-3-запуск-и-дальнейшая-работа)
*   [❓ FAQ (Частые проблемы и их решения)](#-faq-частые-проблемы-и-их-решения)

---

## 🚀 Функционал и возможности

*   🔐 **Абсолютная приватность:** Бот работает только с вашим Telegram ID.
*   💡 **Управление проектами:** Полный цикл ведения проектов со статусами "Идея" -> "Активный" -> "Архив".
*   ✍️ **Редактирование:** Возможность изменять название и описание уже созданных проектов.
*   🔗 **Интеграция с Notion:** Автоматическое создание и обновление страниц для каждого проекта.
*   🤖 **Креативный ИИ-помощник:**
    *   **Генерация мудбордов:** Мгновенная визуализация идей с помощью **Fal.AI**.
    *   **Генерация текстов:** Создание профессиональных описаний для проектов и постов для соцсетей с помощью **Gemini 2.5**.
*   📄 **Создание PDF-отчетов:** Автоматическая генерация PDF-карточек проектов на основе ваших кастомных HTML/CSS шаблонов.

    > Для создания идеальных шаблонов я разработал специальный плагин для Figma
    >
    > ✨ **[WeasyprintReady — Figma to HTML Converter](https://github.com/kkrugley/WeasyprintReady.git)** ✨
    >
    > Он позволяет вам автоматически конвертировать макет Figma в корректные `template.html` и `style.css` с уже проставленными переменными Jinja2. Это практически полностью устраняет необходимость ручной верстки.

*   🎨 **Менеджер шаблонов:** Возможность загружать и хранить неограниченное количество HTML/CSS шаблонов.
*   🔔 **Проактивная система напоминаний:**
    *   Настраиваемые напоминания для **активных проектов**, чтобы не срывать дедлайны.
    *   Еженедельные "мотивирующие" напоминания для работы со списком **идей**.
*   ⚙️ **Продуманный интерфейс:** Интуитивная навигация с кнопками и пошаговыми сценариями (FSM).

## 🛠️ Технологический стек

*   **Язык:** Python 3.12+
*   **Telegram Bot Framework:** aiogram 3.x
*   **Базы данных:**
    *   **Локально:** SQLite (через `aiosqlite`)
    *   **Для деплоя:** PostgreSQL (через `asyncpg` и `psycopg2-binary`)
*   **Миграции БД:** Alembic
*   **PDF Генерация:** WeasyPrint
*   **Планировщик:** APScheduler
*   **Конфигурация:** python-dotenv, Pydantic

## 📋 Что необходимо для работы

1.  **Python 3.12** или выше.
2.  **Git** для контроля версий.
3.  **Heroku CLI** для управления деплоем (опционально, но рекомендуется).
4.  **API ключи**, которые будут храниться в `.env` файле (для локальной разработки) или в Config Vars (на Heroku).
5.  **Настроенная база данных в Notion** с колонками для **Названия** (тип `Title`) и **Статуса** (тип `Select`).

## ⚙️ Локальная установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/kkrugley/designflow_assistant.git
    cd designflow_assistant
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Для macOS/Linux
    python3 -m venv venv
    source venv/bin/activate

    # Для Windows
    python -m venv venv
    venv\Scripts\activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Настройте переменные окружения:**
    *   Создайте файл `.env` в корне проекта.
    *   Заполните его вашими API-ключами и настройками:
    ```dotenv
    # Telegram
    BOT_TOKEN=your_telegram_bot_token
    TELEGRAM_USER_ID=your_personal_telegram_id

    # Notion
    NOTION_API_KEY=your_notion_integration_token
    NOTION_DB_ID=your_notion_database_id
    NOTION_TITLE_PROPERTY_NAME=Name
    NOTION_STATUS_PROPERTY_NAME=Status

    # Generative AI
    LLM_API_KEY=your_gemini_or_openrouter_key
    LLM_API_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent
    FAL_API_KEY=your_fal_ai_key
    ```

5.  **Примените миграции базы данных:**
    ```bash
    # Применить все миграции к базе данных SQLite
    alembic upgrade head
    ```

6.  **Запустите бота:**
    ```bash
    python -m bot
    ```

## ☁️ Развертывание (Деплой) на Heroku

Этот бот готов к развертыванию на Heroku с использованием PostgreSQL и автоматическими деплоями с GitHub.

### Шаг 1: Подготовка файлов проекта

Убедитесь, что в вашем репозитории есть следующие файлы:

1.  **`requirements.txt`**: Должен содержать `psycopg2-binary`, `asyncpg` и `gunicorn`.
2.  **`Procfile`** (без расширения): Файл с одной строкой, указывающий Heroku, как запустить бота.
    ```
    worker: python -m bot
    ```
3.  **`Aptfile`** (без расширения): Файл для установки системных зависимостей для `WeasyPrint`.
    ```
    libpango-1.0-0
    libpangoft2-1.0-0
    ```

### Шаг 2: Настройка Heroku

Эти действия выполняются один раз для нового приложения.

1.  **Создайте приложение** на Heroku: `heroku create ваше-уникальное-имя-бота`.
2.  **Добавьте аддон PostgreSQL:** `heroku addons:create heroku-postgresql:essential-0 -a ваше-имя-бота`.
3.  **Настройте Buildpacks (порядок важен!):**
    ```bash
    heroku buildpacks:add --index 1 heroku-community/apt -a ваше-имя-бота
    heroku buildpacks:add --index 2 heroku/python -a ваше-имя-бота
    ```
4.  **Настройте переменные окружения (Config Vars):**
    *   Перейдите в дашборд вашего приложения на Heroku -> Settings -> Reveal Config Vars.
    *   **Вручную** добавьте все ключи и значения из вашего локального `.env` файла. **Heroku не читает `.env`!**
5.  **Настройте автоматический деплой:**
    *   Перейдите на вкладку Deploy.
    *   В разделе "Deployment method" выберите GitHub и подключите ваш репозиторий `kkrugley/designflow_assistant`.
    *   Включите **"Enable Automatic Deploys"** для ветки `main`.

### Шаг 3: Запуск и дальнейшая работа

1.  **Запустите первый деплой:** Сделайте `git push` в ветку `main` на GitHub или нажмите "Deploy Branch" в интерфейсе Heroku.
2.  **Включите Worker:** После успешной сборки перейдите на вкладку **Resources** и включите `worker`, переместив ползунок с 0 на 1 (или выполните в терминале `heroku ps:scale worker=1 -a ваше-имя-бота`).
3.  **Примените миграции к удаленной БД:**
    ```bash
    heroku run alembic upgrade head -a ваше-имя-бота
    ```
4.  **Проверьте логи:**
    ```bash
    heroku logs --tail -a ваше-имя-бота
    ```
5.  **Дальнейшая работа:** Просто делайте `git push` на GitHub. Heroku будет автоматически обновлять приложение. Не забывайте вручную применять миграции, если меняете структуру БД.

## ❓ FAQ (Частые проблемы и их решения)

**Проблема:** Приложение "крэшится" на Heroku с ошибкой `ModuleNotFoundError: No module named 'asyncpg'`.
*   **Решение:** Убедитесь, что `asyncpg` добавлен в ваш `requirements.txt` и сделайте новый деплой.

**Проблема:** Команда `heroku run alembic upgrade head` падает с ошибкой `MissingGreenlet` или `InvalidRequestError`.
*   **Решение:** Убедитесь, что ваш файл `migrations/env.py` настроен на использование **синхронного** драйвера (`postgresql+psycopg2`) специально для Alembic, в то время как основное приложение использует асинхронный.

**Проблема:** В логах Heroku ошибка `at=error code=H14 desc="No web processes running"`.
*   **Решение:** Это **нормально**. Ваш бот работает как `worker`, а не как веб-сайт. Эту ошибку можно игнорировать.

**Проблема:** Бот падает с ошибкой `OSError: cannot load library 'libgobject-2.0-0'` на Windows.
*   **Решение:** Установите **GTK3 for Windows** и убедитесь, что при установке была добавлена переменная `PATH`.
